{
  "name": "bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve-bot": "mkdir -p ../../logs && npx tsx index.ts 2>&1 | tee -a ../../logs/bot.log",
    "dev": "mkdir -p ../../logs && npx tsx index.ts 2>&1 | tee -a ../../logs/bot.log",
    "build": "tsup index.ts --format cjs --out-dir dist --clean",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "node-telegram-bot-api": "^0.66.0",
    "pino": "^9.9.0",
    "pino-pretty": "^13.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "@types/node-telegram-bot-api": "^0.64.8",
    "tsup": "^8.1.0",
    "tsx": "^4.19.4",
    "typescript": "^5.9.2"
  }
}
