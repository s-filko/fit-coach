# Bug Fixes - February 13, 2025

## Summary
Fixed critical issues in the registration flow that were preventing proper user onboarding and transition to workout planning.

## Issues Fixed

### 1. Weight Not Being Saved (Decimal Values)
**Problem**: User entered weight 72.5 kg, but it was saved as `null` in the database.

**Root Cause**: The `weightSchema` in `registration.validation.ts` used `.int()` constraint, which rejected decimal values.

**Solution**: 
- Removed `.int()` constraint from `weightSchema` to accept decimal values
- Added test case to verify decimal weight values (72.5 kg) are properly saved

**Files Changed**:
- `apps/server/src/domain/user/services/registration.validation.ts`
- `apps/server/tests/integration/services/registration.integration.test.ts`

---

### 2. Profile Status Transition Without Complete Data
**Problem**: `profile_status` could transition to "complete" even when required fields were missing.

**Analysis**: The validation logic was already correct - it checks that all 6 required fields are present AND user has confirmed before marking profile as complete (line 92 in `registration.service.ts`).

**Verification**: No code changes needed. The existing validation in `RegistrationService.processUserMessage()` already prevents transition without all required data.

---

### 3. No Transition to Planning Phase After Registration
**Problem**: After completing registration, the system stayed in chat phase instead of automatically transitioning to session planning phase.

**Root Cause**: In `chat.routes.ts`, the phase transition was hardcoded as `registration → chat` instead of `registration → session_planning`.

**Solution**:
- Changed phase transition from `registration → chat` to `registration → session_planning`
- Added validation for `registration → session_planning` transition in `ChatService.validatePhaseTransition()`
- Updated system note to "Registration complete. Let's plan your first workout!"

**Files Changed**:
- `apps/server/src/app/routes/chat.routes.ts` (line 84)
- `apps/server/src/domain/user/services/chat.service.ts` (added validation)

---

### 4. Double Greeting Issue
**Problem**: User received two greetings:
1. Hardcoded template from Telegram bot: "👋 Hi filko! I'm your personal fitness coach..."
2. LLM-generated greeting from the registration prompt

**Root Cause**: The `/start` command in the Telegram bot sent a hardcoded greeting, then the user's first message triggered another greeting from the LLM.

**Solution**:
- Removed hardcoded greeting from Telegram bot's `/start` handler
- Bot now sends "hi" message to LLM to get a single, personalized greeting
- LLM generates contextual greeting based on the registration prompt

**Files Changed**:
- `apps/bot/handlers.ts`

**Benefits**:
- Single, personalized greeting from LLM
- Greeting is in the user's language (Russian/English)
- Conversation starts immediately with registration flow

---

## Testing

All tests passing: **225/225**

New test added:
- `should accept decimal weight values (e.g., 72.5 kg)` in `registration.integration.test.ts`

## User Experience Improvements

1. **Decimal weights now work**: Users can enter precise weight values like 72.5 kg
2. **Automatic planning flow**: After registration, users immediately enter workout planning phase
3. **Single personalized greeting**: No more double greetings, LLM provides contextual introduction
4. **Data integrity**: Profile status only transitions when all required fields are present and confirmed

## Commits

### 1. Fix Registration Flow Issues (`d2981d9`)

```
fix: resolve registration flow issues

1. Fix weight validation to accept decimal values (72.5 kg)
2. Fix transition after registration complete (registration → session_planning)
3. Fix double greeting issue (single LLM greeting instead of bot + LLM)

All tests passing (225/225)
```

### 2. Add Flexible Phase Transition (`0163df2`)

```
feat: add flexible phase transition after registration

Allow LLM to decide whether to transition to session_planning or chat
based on user intent after registration is complete.

Changes:
- Add phaseTransition field to registrationLLMResponseSchema
- Update RegistrationService to return phaseTransition
- Update registration prompt to guide LLM on phase transition logic
- Update chat.routes.ts to use LLM-decided phase transition
- Add validation for registration → chat transition
- Add tests for both transition scenarios

User can now:
- Go directly to session_planning: "да, давай начнем тренировку"
- Go to chat first: "да, но сначала хочу задать вопросы"

LLM reads user intent and chooses appropriate next phase.
Default is session_planning (most users want to start right away).

All tests passing (227/227)
```

---

## Additional Feature: Flexible Phase Transition

After the initial fixes, we added a more sophisticated feature: **LLM-driven phase transition**.

### Problem
The system was hardcoded to always transition from `registration → session_planning`. This didn't account for users who might want to:
- Ask questions before starting training
- Learn more about the program
- Just chat about fitness first

### Solution
Added `phaseTransition` field to the registration LLM response schema, allowing the LLM to decide the next phase based on user intent.

**How it works:**
1. When registration is complete, LLM analyzes user's confirmation message
2. LLM decides whether user wants to:
   - Start training immediately → `session_planning`
   - Chat first → `chat`
3. System transitions to the appropriate phase

**Examples:**
- User: "да, давай начнем тренировку" → LLM chooses `session_planning`
- User: "да, но сначала хочу задать вопросы" → LLM chooses `chat`
- User: "подтверждаю" (ambiguous) → LLM defaults to `session_planning`

**Files Changed:**
- `apps/server/src/domain/user/services/registration.validation.ts` - Added `phaseTransition` to schema
- `apps/server/src/domain/user/services/registration.service.ts` - Return `phaseTransition`
- `apps/server/src/domain/user/ports/service.ports.ts` - Updated interface
- `apps/server/src/domain/user/services/prompt.service.ts` - Updated prompt with transition logic
- `apps/server/src/app/routes/chat.routes.ts` - Use LLM-decided transition
- `apps/server/src/domain/user/services/chat.service.ts` - Added validation for `registration → chat`
- `apps/server/tests/integration/services/registration.integration.test.ts` - Added tests

**Benefits:**
- More natural conversation flow
- Respects user intent
- Flexible onboarding experience
- LLM handles edge cases automatically
